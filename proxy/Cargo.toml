[package]
name = "tap-tunnel-proxy"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "tap-tunnel-proxy"
path = "src/main.rs"

[dependencies]
tap-tunnel = { path = ".." }
clap = { version = "4", features = ["derive"] }
env_logger = { workspace = true }
libc = { workspace = true }
log = { workspace = true }
tokio = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ipnet = "2"

[target.'cfg(target_os = "linux")'.dependencies]
nix = { version = "0.29", features = ["sched"] }
netconfig-rs = "0.1"
rtnetlink = "0.16"
futures = "0.3"
