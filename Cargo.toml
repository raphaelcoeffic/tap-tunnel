[package]
name = "tap-tunnel"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.29", features = ["sched", "poll", "ioctl", "net"] }
libc = "0.2"
tokio = { version = "1", features = ["rt", "net", "io-util"] }
